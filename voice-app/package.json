{
  "name": "mhtalk-voice",
  "private": true,
  "version": "0.9.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1430",
    "build": "tsc && vite build",
    "tauri": "tauri",
    "tauri:build": "tauri build --no-bundle",
    "build:sidecar": "npm run build && npm run tauri:build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4",
    "typescript": "~5.8.3",
    "vite": "^7.0.0"
  }
}
