# MHTalk Android

The standalone Android client for MHTalk voice rooms, private calls, chat, camera and screen sharing.

Build a debug APK with:

```powershell
.\gradlew.bat :app:assembleDebug
```

Official Android releases are published independently from the Windows client.
