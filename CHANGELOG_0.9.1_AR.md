# MHTalk 0.9.1 — استعادة صوت البث وإصلاح السحب والإفلات

## استمرارية صوت البث

- ربط صوت مشاركة الشاشة بجلسة المشاهدة المفتوحة فقط؛ زر `Close Stream` يوقف التشغيل، ويفصل `srcObject` ومسار Web Audio، ويغلق PiP فوراً من دون قطع صوت المكالمة.
- إلغاء مشغلات صوت الشاشة المخفية لكل المشاركين؛ يوجد الآن مشغل واحد فقط للبث الذي يشاهده المستخدم، ويُنظّف تلقائياً عند الإغلاق أو التبديل.
- إصلاح السبب الجذري لاختفاء صوت مشاركة الشاشة بعد انقطاع اتصال الإشارات وعودته.
- حفظ رسائل WebRTC الخفيفة والمؤقتة (`SDP` و`ICE`) ضمن طابور محدود أثناء إعادة الاتصال، ثم إرسالها بالترتيب بعد استعادة جلسة الغرفة.
- إعادة مزامنة حالة المايك والكاميرا ومشاركة الشاشة تلقائياً بعد نجاح الانضمام مجدداً، من دون تمرير أي حمولة ثقيلة عبر WebSocket.
- استعادة مسار الصوت أو الفيديو المتوقف بصورة مستقلة، وتحديث مرسل صوت الشاشة عند تبدّل المسار.
- منع تراكم أكثر من مسار واحد من النوع نفسه داخل اتصال النظير وإعادة ربط Web Audio عند تغيّر مسار الصوت.
- تسلسل تشغيل وإيقاف محرك الصوت الأصلي لمنع سباقات دورة الحياة أثناء الانقطاع والاستعادة.
- تسلسل رسائل SDP وICE لكل نظير لمنع سباقات التفاوض بعد reconnect.
- إعادة إنشاء اتصال الصوت للنظير فقط إذا تغيّرت سلالة SDP وأصبح ترتيب `m-lines` غير متوافق، بدلاً من مطالبة المستخدم بمغادرة الاتصال.
- تأكيد فقدان heartbeat بفحص Native نشط و`PING/PONG` قبل إعادة تشغيل MHTalkVoice، لتجنب إعادة التشغيل الخاطئة عند تأخر مؤقت WebView2 المخفي.
- تطبيق الاستعادة الفورية نفسها على اتصال WebRTC الرئيسي: اختلاف ترتيب `m-lines` يعيد بناء نقل النظير في دورة الرسالة نفسها بدلاً من انتظار ICE watchdog.
- دمج تحديثات `hello/profile/media` أثناء انقطاع signaling في refresh خفيف واحد بعد reconnect بدلاً من إسقاطها وتكرار السجل.
- إعادة محاولة نشر صورة الحساب تلقائياً عند أخطاء الشبكة المؤقتة فقط عبر React Query، من دون polling أو إعادة محاولة أخطاء HTTP الدائمة.

## السحب والإفلات

- إصلاح تعارض معالج السحب الأصلي في Tauri مع معالج الملفات في واجهة React على Windows.
- تعطيل Tauri drag-drop للنافذة الرئيسية فقط كي تصل أحداث HTML5 إلى منطقة المحادثة كما صُممت.
- تحسين اكتشاف سحب الملفات عبر `DataTransfer.types` مع الإبقاء على التحقق الأمني والحجم ومسار نقل الملفات الحالي.
- نقل بطاقة الملف إلى رسالة الشات فور أول Enter وتحديث نفس الرسالة بشريط التقدم والنجاح أو الفشل، من دون إعادة رفع عند ضغط Enter مجدداً.

## الإصدار والتحقق

- ترقية جميع manifests ومصادر وقت التشغيل المتناسقة إلى `0.9.1`.
- جعل فحوصات الإنتاج وملف الاختبار وملاحظات التحديث التلقائي تعتمد على رقم الإصدار الحالي بدلاً من اسم ثابت لإصدار سابق.
- حصر حزمة المصدر في ملفات الإصدار `0.9.1` وحذف وثائق الإصدارات السابقة ونواتج البناء والاعتماديات القابلة لإعادة التوليد.
- التحقق عبر مسار المشروع: `npm run build` و`npx tsc --project tsconfig.json --noEmit`، إضافة إلى فحوصات المعمارية والصوت والـWorker وRust المقفلة.

## التوافق

تطبيق سطح المكتب ومحرك MHTalkVoice وCloudflare Worker في `0.9.1` مجموعة إصدار متناسقة. ينشر سكربت الإصدار GitHub Draft ويتحقق من أصوله قبل نشر الـWorker، ثم يحوّل الإصدار إلى Latest بعد نجاح السلسلة كاملة.
